# CVXPY estimator model settings only
# Data path is passed via CLI: --data /path/to/dataset.parquet

EST_FEATURES=delta_cycles,delta_cache_misses,delta_branch_instructions,delta_instructions,syscall_class_other
EST_L1_PENALTY=0.1
EST_STATIC_PENALTY=0.0
